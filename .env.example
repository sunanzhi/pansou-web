# 后端 API 根地址 (末尾不带斜杠)
# 留空时默认使用当前站点的相对路径 /api (适用于由同一域名反代或本地测试)
# 部署至 Cloudflare Pages 时，可配置为您 Cloudflare Tunnel 绑定的后端域名，例如:
# VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=

# 开发模式下 Vite 本地代理目标后端端口
VITE_API_PROXY_TARGET=http://localhost:8888
